# Runtime data generated by config.py / database.py — never committed.
data/
logs/*.log
!logs/.gitkeep

# Local environment overrides (DB credentials, secret key, etc.)
.env

# PyInstaller build output
build/
dist/
*.spec
